# Production overrides — only what differs from values.yaml

replicaCount: 3

image:
  tag: "stable"

resources:
  requests:
    cpu: "512m"
    memory: "1024Mi"
  limits:
    cpu: "1000m"
    memory: "2048Mi"

autoscaling:
  minReplicas: 3
  maxReplicas: 10
