apiVersion: v1
kind: Service
metadata:
  name: cloudops-api-service
  namespace: default
spec:
  selector:
    app: cloudops-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
